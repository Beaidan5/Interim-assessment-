// Root server.js — Entry point for Render deployment
// Starts the backend server from the root directory

require('dotenv').config();

// Import and start the backend server
require('./backend/server.js');
